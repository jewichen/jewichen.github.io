# 個人 GitHub Pages 網站

## 使用方式
1. 將 `index.html` 上傳到你的 `username.github.io` repository 根目錄。
2. Commit changes。
3. 等待約 1–3 分鐘後重新整理你的 GitHub Pages URL。

## 日後修改
目前全部樣式都放在同一個 `index.html` 中，方便剛開始直接修改。
之後若內容變多，可以再拆成 `style.css` 與 `script.js`。
